import * as React from "react";
import { Button } from "./button";

export default function ProjectButton() {
  return (
    <div>
      <Button className="bg-blue-600 h-[40px] py-2 px-4 rounded-md text-sm"></Button>
    </div>
  );
}
